# Copy this file to .env and fill with your secrets
TELEGRAM_BOT_TOKEN=
GEMINI_API_KEY=
# SQLite file path (absolute path recommended in Docker)
DATABASE_URL=sqlite:////data/finance.db
# Comma-separated Telegram user ids who can manage categories
ADMINS=
# Optional: restrict bot to a specific group chat id (e.g., -1001234567890)
ALLOWED_CHAT_ID=
# Default currency symbol for display
DEFAULT_CURRENCY=₽
# Locale for week start (monday/sunday). Only affects labels, not timezone logic.
WEEK_START=monday